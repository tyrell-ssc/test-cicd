{
  "name": "@test-cicd/db",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "drizzle-kit generate",
    "push": "drizzle-kit push && bun run migrate",
    "migrate": "cd ../supabase && bunx supabase migration up && cd ../db",
    "studio": "drizzle-kit studio"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1",
    "postgres": "^3.4.5",
    "dotenv": "^16.4.5",
    "nanoid": "^5.1.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.8",
    "@types/node": "^20.10.6"
  }
}
