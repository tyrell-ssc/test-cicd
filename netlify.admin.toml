# Netlify Configuration for Admin Panel
[build]
  base = "."
  command = "curl -fsSL https://bun.sh/install | bash && export PATH=$HOME/.bun/bin:$PATH && bun install && bun run build:admin:production"
  publish = "apps/admin/dist"

[build.environment]
  NODE_VERSION = "20"

# Production (main branch)
[context.production]
  command = "curl -fsSL https://bun.sh/install | bash && export PATH=$HOME/.bun/bin:$PATH && bun install && bun run build:admin:production"

[context.production.environment]
  VITE_ENVIRONMENT = "production"

# Staging (staging branch)
[context.staging]
  command = "curl -fsSL https://bun.sh/install | bash && export PATH=$HOME/.bun/bin:$PATH && bun install && bun run build:admin:staging"

[context.staging.environment]
  VITE_ENVIRONMENT = "staging"

# Deploy Previews (PRs)
[context.deploy-preview]
  command = "curl -fsSL https://bun.sh/install | bash && export PATH=$HOME/.bun/bin:$PATH && bun install && bun run build:admin:production"

[context.deploy-preview.environment]
  VITE_ENVIRONMENT = "preview"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
